import { TwelvehourformatPipe } from './twelvehourformat.pipe';

describe('TwelvehourformatPipe', () => {
  it('create an instance', () => {
    const pipe = new TwelvehourformatPipe();
    expect(pipe).toBeTruthy();
  });
});
