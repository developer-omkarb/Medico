import { ClassChangerDirective } from './class-changer.directive';

describe('ClassChangerDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassChangerDirective();
    expect(directive).toBeTruthy();
  });
});
